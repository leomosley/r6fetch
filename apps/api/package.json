{
  "name": "@r6fetch/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "sh ../../scripts/build-rust-worker.sh",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "cargo check --manifest-path ../../Cargo.toml -p r6-api --target wasm32-unknown-unknown"
  },
  "devDependencies": {
    "wrangler": "^4.12.0"
  }
}
